<h1><%= @deck.name %></h1>

<%= button_to "User Homepage", user_path(current_user[:id]),method: 'get'%>

<div class="card_gallery">
  <% @deck.cards.each do |card| %>
    <%=image_tag("#{card.img}", class: "display_card_image")%>
  <%end%>
</div>
